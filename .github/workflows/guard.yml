on: [pull_request]

jobs:
  guard:
    runs-on: ubuntu-latest
    name: Guard
    steps:
      - name: CFN Guard Action
        uses: dannyvassallo/cloudformation-guard@v1.18
        with:
          rules: './guard/resources/validate/rules-dir/'
          data: './guard/resources/validate/data-dir/'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
